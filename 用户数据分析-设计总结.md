# 用户数据分析功能 - 核心设计总结

> 基于 PRD v1.1 - 快速参考版

## 🎯 核心设计

### 实现位置
在现有 `/admin/dashboard` 添加新标签页："用户数据分析"

---

## 📊 高价值用户定义（更新）

### 新定义
```
高价值用户 = 分析次数 > 3  AND  30天内有登录
```

**设计思路**:
- **分析次数 > 3**: 用户有实际使用，产生了价值
- **30天内有登录**: 用户在近期内仍然活跃，没有完全流失
- 两个条件**同时满足**，筛选出有使用量且保持一定活跃度的用户

---

## 🏷️ 用户状态标签

| 标签 | 条件 | 颜色 |
|------|------|------|
| **高价值** | 分析次数 > 3 且 30天内有登录 | 紫色 |
| **活跃** | 7天内有登录 | 绿色 |
| **沉默** | 30天未登录 | 灰色 |
| **已耗尽** | 剩余额度 = 0 | 红色 |

**注意**: 一个用户可以同时拥有多个标签

---

## 📋 用户列表字段

| 字段 | 说明 | 格式 |
|------|------|------|
| 手机号 | 完整显示 | 13800138000 |
| 姓名 | 用户姓名 | 张三 / 未设置 |
| 分析次数 | 该用户创建的分析记录总数 | 数字 |
| 最后登录 | 相对时间 | 2小时前 / 昨天 / 7天前 |
| 状态 | 标签 | 高价值 / 活跃 / 沉默 / 已耗尽 |
| 操作 | 按钮 | 查看详情 |

---

## 🔍 筛选功能

| 筛选项 | 说明 | 查询条件 |
|--------|------|----------|
| 全部用户 | 显示所有 | 无 |
| 高价值用户 | 分析 > 3 且 30天内有登录 | `COUNT > 3 AND last_login >= 30天前` |
| 活跃用户 | 7天内有登录 | `last_login >= 7天前` |
| 沉默用户 | 30天未登录 | `last_login < 30天前` |
| 已耗尽额度 | 剩余额度 = 0 | `remaining_quota = 0` |

**搜索**: 支持手机号、姓名实时搜索

---

## 📈 统计卡片（3个）

1. **总用户数**: `User.query.count()`
2. **高价值用户**: COUNT(分析次数 > 3 且 7天内有登录)
3. **今日活跃**: 今天有登录或分析的用户数

---

## 💬 用户详情弹窗

点击"查看详情"显示：

**基础信息**:
- 姓名、手机号
- 注册时间、最后登录

**使用数据**:
- 分析次数（总数）
- 剩余额度 / 总额度
- 使用率百分比

**最近活动**:
- 最近10条分析记录
- 显示项目名称和创建时间

---

## 🛠️ 技术实现要点

### 后端API
```python
GET  /admin/users-analytics           # 页面
GET  /admin/api/users/analytics/stats     # 统计数据
GET  /admin/api/users/analytics/list     # 用户列表
GET  /admin/api/users/<id>/detail        # 用户详情
GET  /admin/api/users/export            # 导出CSV
```

### 数据库查询
```sql
-- 高价值用户查询
SELECT u.*, COUNT(ar.id) as analysis_count
FROM users u
LEFT JOIN analysis_results ar ON u.id = ar.user_id
WHERE u.last_login >= DATE('now', '-30 days')
GROUP BY u.id
HAVING COUNT(ar.id) > 3
```

### 需要添加的索引
```sql
CREATE INDEX idx_users_last_login ON users(last_login);
```

---

## ✅ 开发范围（Phase 1）

### 本次开发功能

- [ ] 用户列表展示（5个字段）
- [ ] 统计卡片（3个指标）
- [ ] 筛选功能（5种筛选方式）
- [ ] 搜索功能（手机号/姓名）
- [ ] 排序功能（默认按分析次数降序，支持切换）
- [ ] 用户详情弹窗（基础信息 + 使用数据 + 最近10条记录）
- [ ] 数据导出（CSV格式）
- [ ] 分页功能（每页20条）

### 暂不开发

- ❌ 图表可视化（用户增长趋势、活跃度分布等）
- ❌ 自定义筛选规则
- ❌ 批量操作功能
- ❌ 高级数据分析（用户分群、预测分析等）

---

## 📁 文件清单

### 需要新建的文件
- `templates/admin_users_analytics.html` - 用户数据分析页面

### 需要修改的文件
- `app.py` - 添加5个新路由
- `models.py` - 添加数据库索引（可选）

---

## ⏱️ 预计工作量

- **后端开发**: 4-6小时
- **前端开发**: 4-6小时
- **测试联调**: 2-3小时
- **总计**: 1-2天

---

## 🎨 界面设计要点

### 设计风格
- 保持与现有管理员后台一致
- 使用 Apple 风格的卡片和表格设计
- 状态标签使用渐变色

### 关键代码
```html
<!-- 标签页导航 -->
<ul class="nav nav-tabs">
    <li class="nav-item">
        <a class="nav-link" href="/admin/dashboard">概览</a>
    </li>
    <li class="nav-item">
        <a class="nav-link active" href="/admin/users-analytics">
            <i class="fas fa-chart-line"></i> 用户数据分析
        </a>
    </li>
</ul>

<!-- 统计卡片 -->
<div class="stats-card">
    <div class="stat-item">
        <span class="stat-number" id="total-users">-</span>
        <span class="stat-label">总用户数</span>
    </div>
</div>
```

---

## 📝 验收标准

- [ ] 可以在管理员后台看到"用户数据分析"标签页
- [ ] 统计卡片显示正确的数据
- [ ] 筛选功能正常工作
- [ ] 排序功能正常（默认按分析次数降序）
- [ ] 用户详情弹窗显示正确信息
- [ ] 导出CSV功能正常
- [ ] 页面加载时间 < 2秒

---

**文档版本**: v1.1 Summary
**创建日期**: 2026-01-06
**完整PRD**: `用户数据分析功能PRD.md`
